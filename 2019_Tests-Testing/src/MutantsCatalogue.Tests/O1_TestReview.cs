using System;
using System.Collections.Generic;
using System.Text;
using Xunit;

namespace MutantsCatalogue.Tests
{
    public class TestReview
    {
        [Fact]
        public void Case1_MultipleAssert()
        {
            // arrange

            dynamic mutantsService = null;
            dynamic myenum = 0;
            // act

            dynamic result = mutantsService.GetMutant(myenum.Wolferine);
            // assert
            Assert.Equal("Logan", result.RealName);
            Assert.Equal(100.00, result.Weight);
            Assert.Equal(186.4, result.Height);
            Assert.Equal("Wolferine", result.NickName); // issue (copy if null)
            Assert.Null(result.BirthDay); 
            Assert.Null(result.Age); // here we've forgotten to check if age is not defined
        }
        /* 
         * В данном случае мы просто пропустили один из кейсов
         * 
         */

        [Fact]
        public void AutogeneratedAssert()
        {
            // arrange

            // act
            dynamic result = null;
            // assert
            Assert.Equal($"{result} {result.BirthDay}",
                result.ToString());
        }
       // если пользователь отправит не UTC, то формат правлится


        // todo: пример с ревью с тестов
        // когда мы начали ревью теста, почитали требования и поняли что кое что пропущено
        // с тестов начать проще так как они отвечают на вопрос - зачем,
        // пример: REGEXP




    }
}
